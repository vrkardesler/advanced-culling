buildscript {
    repositories {
        maven { url = 'https://maven.minecraftforge.net/' }
        maven { url = 'https://repo.spongepowered.org/repository/maven-public/' }
        mavenCentral()
    }
    dependencies {
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '5.1.+', changing: true
        classpath 'org.spongepowered:mixingradle:0.7-SNAPSHOT'
    }
}

apply plugin: 'net.minecraftforge.gradle'
apply plugin: 'org.spongepowered.mixin'
apply plugin: 'eclipse'

version = '1.0.0'
group = 'com.universal.culling'
archivesBaseName = 'advculling'

// --- 1. HATANIN ÇÖZÜMÜ: DOĞRU TOOLCHAIN TIP TANIMLAMASI ---
java.toolchain.languageVersion = JavaLanguageVersion.of(17)

dependencies {
    minecraft 'net.minecraftforge:forge:1.20.4-49.+'
    annotationProcessor 'org.spongepowered:mixin:0.8.5:processor'
}

// --- 2. HATANIN ÇÖZÜMÜ: BAĞIMLILIK SONRASI SIRA DÜZENLEMESİ ---
minecraft {
    mappings channel: 'official', version: '1.20.4'
    runs {
        client {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            arg "-mixin.config=advculling.mixins.json"
            mods {
                advculling {
                    source sourceSets.main
                }
            }
        }
    }
}

mixin {
    add sourceSets.main, "advculling.refmap.json"
}